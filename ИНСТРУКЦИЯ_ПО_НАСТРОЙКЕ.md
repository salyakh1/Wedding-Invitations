# 🔧 ИНСТРУКЦИЯ: Настройка таблицы wishes для слайдера пожеланий

## ⚡ БЫСТРЫЙ СПОСОБ (Рекомендуется)

### Шаг 1: Откройте Supabase Dashboard
1. Перейдите на https://supabase.com/dashboard
2. Войдите в свой аккаунт
3. Выберите проект **wedding-invitations**

### Шаг 2: Откройте SQL Editor
1. В левом меню найдите **"SQL Editor"** (иконка с символом `</>` или база данных)
2. Нажмите на него

### Шаг 3: Скопируйте и выполните код
Скопируйте **ВЕСЬ** код из файла `wishes-quick-setup.sql` и вставьте в SQL Editor, затем нажмите кнопку **"Run"** (Запустить)

### Шаг 4: Проверка
После выполнения должны быть видны сообщения об успехе. Если есть ошибки:
- Если таблица уже существует - это нормально (используется `IF NOT EXISTS`)
- Если политики уже существуют - они будут пересозданы

---

## 📝 ЧТО ДЕЛАЕТ ЭТОТ SQL КОД:

1. **Создает таблицу `wishes`** с полями:
   - `id` - уникальный идентификатор
   - `invitation_id` - связь с приглашением
   - `name` - имя гостя
   - `message` - текст пожелания
   - `created_at` - дата создания

2. **Создает индекс** для быстрого поиска по `invitation_id`

3. **Включает RLS** (Row Level Security)

4. **Создает политики доступа**:
   - Любой может **читать** пожелания (SELECT)
   - Любой может **добавлять** пожелания (INSERT)

---

## ✅ КАК ПРОВЕРИТЬ ЧТО ВСЕ РАБОТАЕТ:

1. Выполните в SQL Editor:
```sql
SELECT COUNT(*) FROM wishes;
```
Должно вернуться число (может быть 0, если пожеланий еще нет)

2. Проверьте политики:
```sql
SELECT policyname FROM pg_policies WHERE tablename = 'wishes';
```
Должно показать 2 политики

---

## 🎯 ВАЖНО:

После выполнения этих SQL запросов:
- ✅ Таблица `wishes` будет создана
- ✅ Гости смогут оставлять пожелания (блок "Пожелания")
- ✅ Пожелания будут автоматически показываться в слайдере (блок "Слайдер пожеланий")

Готово! Теперь пожелания будут работать! 🎉

