# 📦 НАСТРОЙКА SUPABASE STORAGE ДЛЯ ВИДЕО

## Проблема
При сохранении приглашений с видео возникает ошибка **"statement timeout"**, потому что видео сохраняется в base64 в базу данных, что создает очень большие записи.

## Решение
Используем **Supabase Storage** для хранения видео файлов, а в базу данных сохраняем только URL.

---

## 🚀 Пошаговая настройка

### 1. Откройте Supabase Dashboard
- Перейдите на [supabase.com](https://supabase.com)
- Войдите в свой проект `wedding-invitations`

### 2. Откройте SQL Editor
- В левом меню выберите **"SQL Editor"**
- Нажмите **"New query"**

### 3. Выполните SQL скрипт
- Откройте файл `setup-storage-bucket.sql` в проекте
- Скопируйте весь код из файла
- Вставьте в SQL Editor
- Нажмите **"Run"** (или `Ctrl+Enter`)

### 4. Проверьте создание bucket
- В левом меню выберите **"Storage"**
- Должен появиться bucket с именем **"videos"**
- Убедитесь, что он помечен как **"Public"**

### 5. Проверьте политики доступа
- В Storage → videos → Policies
- Должны быть созданы политики:
  - ✅ "Public can view videos" (SELECT)
  - ✅ "Authenticated users can upload videos" (INSERT)
  - ✅ "Users can update their own videos" (UPDATE)
  - ✅ "Users can delete their own videos" (DELETE)

---

## ✅ Готово!

Теперь при загрузке видео:
1. Видео загружается в Supabase Storage
2. В базу данных сохраняется только URL (небольшой текст)
3. Сохранение происходит быстро, без timeout
4. Видео отображается на публичной странице через публичный URL

---

## 🔄 Обратная совместимость

Старые видео, сохраненные в base64, продолжат работать. Новые видео будут загружаться в Storage.

---

## ⚠️ Важно

- Максимальный размер видео: **50MB**
- Поддерживаемые форматы: MP4, WebM, OGG, QuickTime, AVI
- Bucket должен быть **публичным** для чтения видео на публичной странице

