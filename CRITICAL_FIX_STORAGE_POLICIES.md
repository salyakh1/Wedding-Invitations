# 🚨 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Storage Политики

## ⚠️ Проблема
Ошибка "new row violates row-level security policy" возникает потому, что политики применены к роли **"public"**, а не к **"authenticated"**.

## ✅ РЕШЕНИЕ (ОБЯЗАТЕЛЬНО!)

### Шаг 1: Откройте политики в Supabase Dashboard
1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите в проект `wedding-invitations`
3. Storage → videos → Policies

### Шаг 2: Измените Target Roles для каждой политики

**Для политики INSERT "Authenticated users can upload videos":**
1. Нажмите на три точки (⋮) справа от политики
2. Выберите "Edit"
3. Найдите поле **"Target roles"**
4. **ВАЖНО:** Нажмите на поле "Target roles"
5. **Выберите "authenticated"** из списка (НЕ "public"!)
6. Нажмите "Review" → "Save policy"

**Для политики UPDATE "Authenticated users can update videos":**
1. Повторите те же шаги
2. Выберите "authenticated" в "Target roles"
3. Сохраните

**Для политики DELETE "Authenticated users can delete videos":**
1. Повторите те же шаги
2. Выберите "authenticated" в "Target roles"
3. Сохраните

**Для политики SELECT "Public can view videos":**
- Эту политику можно оставить для "public" (она для публичного чтения)

### Шаг 3: Проверка

После изменения всех политик:
1. Обновите страницу конструктора (F5)
2. Попробуйте загрузить видео
3. Должно работать без ошибок!

---

## 🔍 Как проверить, что политики применены правильно

1. Откройте Storage → videos → Policies
2. Проверьте колонку "APPLIED TO" в таблице политик
3. Для INSERT, UPDATE, DELETE должно быть **"authenticated"**, НЕ "public"!

---

## ⚠️ Если Target Roles нельзя изменить

Если в интерфейсе нет возможности выбрать роль, попробуйте:

1. **Удалите все политики** (кроме SELECT)
2. **Создайте их заново** через кнопку "New policy"
3. При создании новой политики выберите "authenticated" в "Target roles"

---

## 📝 Альтернативное решение (если ничего не помогает)

Если политики все еще не работают, можно временно использовать более простую политику:

1. Откройте политику INSERT
2. Измените WITH CHECK на: `bucket_id = 'videos'`
3. Сохраните
4. **⚠️ ВНИМАНИЕ:** Это позволит загружать видео всем. Используйте только для тестирования!

Но лучше исправить Target Roles на "authenticated"!

