# Wedding Invitation Service

Современный сервис для создания свадебных пригласительных с конструктором.

## Возможности

- 🎨 **Конструктор пригласительных** - Drag & Drop интерфейс
- 🖼️ **Настраиваемый фон** - Загрузка собственных изображений
- 👰 **Блок имен** - Имена жениха и невесты
- 📝 **Текстовые блоки** - Произвольный текст
- 🎥 **Видео блоки** - Встраивание видео с настройкой формы и размера
- 🗺️ **Карта** - Интеграция с Google Maps
- 📖 **История любви** - Текст с фотографиями
- 💬 **Пожелания** - Гости могут оставлять пожелания
- 🎠 **Слайдер пожеланий** - Красивое отображение пожеланий
- 🎵 **Фоновая музыка** - Загрузка аудио файлов
- 🎨 **Настройки стиля** - Шрифты, размеры, прозрачность
- 📱 **Адаптивный дизайн** - Работает на всех устройствах

## Технологии

- **Frontend**: Next.js 14, React 18, TypeScript
- **Styling**: Tailwind CSS, Framer Motion
- **Database**: Supabase (PostgreSQL)
- **Authentication**: Supabase Auth
- **Deployment**: Vercel

## Установка и настройка

### 1. Клонирование проекта

```bash
git clone <repository-url>
cd wedding-invitation-service
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Скопируйте файл `env.example` в `.env.local`
3. Заполните переменные окружения:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 4. Настройка базы данных

Выполните SQL скрипт из файла `supabase-schema.sql` в Supabase SQL Editor:

```sql
-- Создание таблиц и политик безопасности
-- (см. содержимое файла supabase-schema.sql)
```

### 5. Создание пользователя

Создайте пользователя с логином `saleh999` и паролем `salean9595$$`:

1. Перейдите в Supabase Dashboard → Authentication → Users
2. Нажмите "Add user"
3. Email: `saleh999@example.com`
4. Password: `salean9595$$`

### 6. Запуск проекта

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Использование

### Вход в систему

- **Логин**: `saleh999@example.com`
- **Пароль**: `salean9595$$`

### Создание приглашения

1. Нажмите "Новое" для создания приглашения
2. Используйте левую панель для добавления блоков
3. Настройте блоки в правой панели
4. Сохраните изменения
5. Нажмите "Просмотр" для получения ссылки

### Просмотр приглашения гостями

Гости могут просматривать приглашение по ссылке вида:
`https://your-domain.com/invitation/[invitation-id]`

## Структура проекта

```
├── app/
│   ├── components/          # React компоненты
│   │   ├── blocks/         # Компоненты блоков
│   │   ├── ConstructorPanel.tsx
│   │   ├── InvitationCanvas.tsx
│   │   └── SettingsPanel.tsx
│   ├── constructor/        # Страница конструктора
│   ├── invitation/[id]/    # Публичная страница приглашения
│   ├── globals.css         # Глобальные стили
│   ├── layout.tsx          # Основной layout
│   ├── page.tsx           # Страница входа
│   └── providers.tsx      # Провайдеры контекста
├── lib/
│   └── supabase.ts        # Конфигурация Supabase
├── types/
│   └── index.ts           # TypeScript типы
├── supabase-schema.sql    # SQL схема базы данных
└── package.json
```

## Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения в Vercel Dashboard
3. Деплой произойдет автоматически

## Лицензия

MIT License

## Поддержка

Для вопросов и предложений создайте issue в репозитории.
